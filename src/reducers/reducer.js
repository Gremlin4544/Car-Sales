export const initialState = {
    title: 'Car lot sales',
    editing: false
};

export const listReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};